{
  "name": "rmj.io",
  "version": "0.0.1",
  "description": "My personal site front-end built in Angular 2.",
  "main": "./app/boot.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "prebuild": "npm run clean",
    "clean": "rm -rf public/build && mkdir public/build",
    "js:build": "browserify app/boot.ts -p tsify -o public/build/bundle.js",
    "js:watch": "watchify -d app/boot.ts -p tsify -o public/build/bundle.js",
    "lint": "eslint .",
    "release:major": "changelog -M && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version major && git push origin && git push origin --tags",
    "release:minor": "changelog -m && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version minor && git push origin && git push origin --tags",
    "release:patch": "changelog -p && git add CHANGELOG.md && git commit -m 'updated CHANGELOG.md' && npm version patch && git push origin && git push origin --tags",
    "scss:build": "node-sass -o public/build app/styles/index.scss",
    "scss:watch": "npm run scss:build && node-sass -w app/styles -o public/build app/styles/index.scss",
    "serve": "lite-server",
    "prestart": "npm run clean",
    "start": "concurrent 'npm run js:watch' 'npm run scss:watch' 'npm run serve'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robinjoseph08/rmj.io.git"
  },
  "author": "Robin Joseph <robin.joseph@me.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/robinjoseph08/rmj.io/issues"
  },
  "homepage": "https://github.com/robinjoseph08/rmj.io#readme",
  "dependencies": {
    "angular2": "^2.0.0-beta.0",
    "es6-promise": "^3.0.2",
    "es6-shim": "^0.33.13",
    "reflect-metadata": "^0.1.2",
    "rxjs": "^5.0.0-beta.0",
    "zone.js": "^0.5.10"
  },
  "devDependencies": {
    "browserify": "^12.0.1",
    "concurrently": "^1.0.0",
    "eslint": "^1.10.3",
    "eslint-config-lob": "^1.0.1",
    "generate-changelog": "^1.0.0",
    "html2js-browserify": "^1.1.0",
    "lite-server": "^1.3.1",
    "node-sass": "^3.4.2",
    "tsify": "^0.13.1",
    "watchify": "^3.6.1"
  },
  "engine": {
    "node": ">= 0.10.x"
  }
}
